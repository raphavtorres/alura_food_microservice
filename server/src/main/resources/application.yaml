server:
  port: 8081

spring:
  application:
    name: server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://127.0.0.1:8081/eureka
